---
"neon": minor
---

Add the Neon CLI to the monorepo as `packages/cli`, published as the `neon` package. The source is migrated from `neondatabase/neonctl` and is functionally identical to `neonctl@2.27.0`, with the binary named `neon`.
